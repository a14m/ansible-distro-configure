---
- name: "Install pip"
  become: true
  ansible.builtin.package:
    name: "python3-pip"
    state: present

- name: "Install proxmoxer"
  become: true
  ansible.builtin.pip:
    name:
      - "proxmoxer>=2.3.0"
      - "requests"
    state: present
    extra_args: "--break-system-packages"
