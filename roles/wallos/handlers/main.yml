---
- name: "Restart wallos"
  become: true
  ansible.builtin.service:
    name: "wallos"
    state: "restarted"

- name: "Reload systemd"
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true
