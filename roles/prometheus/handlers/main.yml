---
- name: "Restart prometheus"
  become: true
  ansible.builtin.systemd_service:
    name: "prometheus"
    state: "restarted"

- name: "Restart nginx"
  become: true
  ansible.builtin.systemd_service:
    name: "nginx"
    state: "restarted"
