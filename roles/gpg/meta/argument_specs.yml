---
argument_specs:
  main:
    options:
      gpg_public_keys:
        type: "list"
        elements: "str"
        required: false
        description: "List of GPG public keys (ASCII armored) to import"
