---
argument_specs:
  main:
    options:
      nodejs_versions:
        type: "list"
        description: "Node versions to install via mise (e.g. 'lts', 'latest', '22')"
        elements: "str"
        default: ["lts"]
