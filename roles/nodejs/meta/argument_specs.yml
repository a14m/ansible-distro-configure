---
argument_specs:
  main:
    description: "Install nodejs versions via mise on Linux distributions"
    options:
      nodejs_versions:
        type: "list"
        description: "Node versions to install via mise (e.g. 'lts', 'latest', '22')"
        elements: "str"
        default: ["lts"]
