---
argument_specs:
  main:
    options:
      cgit_hostname:
        type: "str"
        description: "FQDN for cgit (e.g. git.example.com)"
        default: "git.example.com"
      cgit_clone_prefix:
        type: "str"
        description: >-
          Hostname used for the SSH clone-prefix (e.g. git.home.arpa). Defaults
          to cgit_hostname; override when cgit_hostname is a public
          reverse-proxy vhost name (HTTP-only) not reachable over SSH.
        default: "git.example.com"
      cgit_port:
        type: "int"
        description: "Port lighttpd listens on internally"
        default: 3000
      cgit_repos_dir:
        type: "str"
        description: "Directory containing bare git repositories"
        default: "/srv/git"
      cgit_title:
        type: "str"
        description: "cgit index page title"
        default: "git"
      cgit_description:
        type: "str"
        description: "cgit index page description"
        default: "personal git repositories"
      cgit_ssh_user:
        type: "str"
        description: "System user for SSH git push access"
        default: "git"
