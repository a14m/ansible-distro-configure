---
argument_specs:
  main:
    options:
      yamllint_version:
        type: "str"
        description: "The default yamllint version to use (must be in major.minor.patch format)"
        default: "1.37.1"
