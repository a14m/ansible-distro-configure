---
argument_specs:
  main:
    options:
      vimrc_repo_url:
        type: "str"
        description: "The vimrc git repo URL"
        required: true
      vimrc_setup_script:
        type: "str"
        description: "The vimrc setup script"
        required: true
