---
- name: "Restart grafana"
  become: true
  ansible.builtin.systemd_service:
    name: "{{ grafana_service }}"
    state: "restarted"

- name: "Restart nginx"
  become: true
  ansible.builtin.systemd_service:
    name: "nginx"
    state: "restarted"
