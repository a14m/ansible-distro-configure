---
argument_specs:
  main:
    options:
      grafana_port:
        description: "Port for Grafana web interface"
        type: "int"
        default: 3000
      grafana_admin_user:
        description: "Grafana admin username"
        type: "str"
        default: "admin"
      grafana_admin_password:
        description: "Grafana admin password"
        type: "str"
        default: "changeme"
      grafana_database_type:
        description: "Database type for Grafana"
        type: "str"
        default: "sqlite3"
        choices:
          - "sqlite3"
          - "mysql"
          - "postgres"
      grafana_datasources:
        description: "List of datasources to provision"
        type: "list"
        elements: "dict"
        default: []
        options:
          name:
            description: "Name of the datasource"
            type: "str"
            required: true
          uid:
            description: "Unique identifier for the datasource to be used in dashboard configurations"
            type: "str"
            required: true
          type:
            description: "Type of datasource"
            type: "str"
            default: "prometheus"
          url:
            description: "URL of the datasource"
            type: "str"
            required: true
          default:
            description: "Whether this is the default datasource"
            type: "bool"
            default: false
      grafana_hostname:
        type: "str"
        description: >-
          FQDN for the grafana vhost (e.g. 'monitor.home.arpa'). Also
          used to set root_url in grafana.ini. Leave undefined to skip
          vhost creation and root_url configuration entirely.
      grafana_dashboards:
        description: "List of dashboards to download and provision"
        type: "list"
        elements: "dict"
        default: []
        options:
          name:
            description: "Local name for the dashboard file"
            type: "str"
            required: true
          url:
            description: "URL to download the dashboard JSON from"
            type: "str"
            required: true
          datasource_mappings:
            description: "List of datasource variable mappings for the dashboard"
            type: "list"
            elements: "dict"
            default: []
            options:
              key:
                description: "Original datasource reference to replace"
                type: "str"
                required: true
              value:
                description: "Target datasource UID to use"
                type: "str"
                required: true
