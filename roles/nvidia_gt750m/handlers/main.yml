---
- name: "Rebuild initramfs"
  become: true
  ansible.builtin.command:
    cmd: "mkinitcpio -P"
  changed_when: true

- name: "Require reboot"
  ansible.builtin.set_fact:
    reboot_required: true
