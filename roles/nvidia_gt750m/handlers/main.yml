---
- name: "Run mkinitcpio"
  become: true
  ansible.builtin.command:
    cmd: "mkinitcpio -P"
  changed_when: true

- name: "Run update-initramfs"
  become: true
  ansible.builtin.command:
    cmd: "update-initramfs -u -k all"
  changed_when: true
