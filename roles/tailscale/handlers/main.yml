---
- name: "Restart tailscale (openrc)"
  become: true
  ansible.builtin.service:
    name: "tailscale"
    state: "restarted"

- name: "Restart tailscaled (systemd)"
  become: true
  ansible.builtin.systemd_service:
    name: "tailscaled"
    state: "restarted"
