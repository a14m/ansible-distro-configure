---
- name: "Restart caddy"
  become: true
  ansible.builtin.service:
    name: "caddy"
    state: "restarted"

- name: "Reload systemd"
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true
