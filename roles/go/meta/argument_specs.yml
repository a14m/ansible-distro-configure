---
argument_specs:
  main:
    options:
      go_versions:
        type: "list"
        description: "Go versions to install via mise (e.g. 'latest', '1.24')"
        elements: "str"
        default: ["latest"]
