---
- name: "Reload rules"
  become: true
  ansible.builtin.command:
    cmd: "udevadm control --reload-rules"
  changed_when: true

- name: "Reboot"
  become: true
  ansible.builtin.reboot:
    reboot_timeout: 600
