---
argument_specs:
  main:
    options:
      dotfiles_repo_url:
        type: "str"
        description: "The dotfiles git repo URL"
        required: true
      dotfiles_setup_script:
        type: "str"
        description: "The dotfiles setup script"
        required: true
