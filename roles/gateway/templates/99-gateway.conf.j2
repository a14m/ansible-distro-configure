{% if gateway_enabled %}
net.ipv4.ip_forward=1
# Raise from 30s: short QUIC flows were reaped before the peer's next packet,
# which then hit INPUT with no NAT entry and got dropped, stalling calls.
net.netfilter.nf_conntrack_udp_timeout=120
{% else %}
net.ipv4.ip_forward=0
{% endif %}
