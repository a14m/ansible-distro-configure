---
- name: "Logs configure"
  gather_facts: true
  hosts:
    - logs.home.arpa
  vars:
    network_ipv4_address: "{{ ansible_facts.default_ipv4.address }}"
  roles:
    - role: "loki"
    - role: "alloy"
