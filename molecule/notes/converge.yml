---
- name: "Proxy configure"
  gather_facts: true
  hosts:
    - proxy.home.arpa
  vars:
    network_ipv4_address: "{{ ansible_facts.default_ipv4.address }}"
  roles:
    - role: "caddy"

- name: "Notes configure"
  gather_facts: true
  hosts:
    - notes.home.arpa
  vars:
    network_ipv4_address: "{{ ansible_facts.default_ipv4.address }}"
    # pasta (rootless podman's default net) gives the container the host's IP,
    # which isn't bindable inside the netns - bind loopback in tests instead.
    # CouchDB's Erlang HTTP listener needs a literal IP here, not a hostname.
    couchdb_bind_address: "127.0.0.1"
    backup_repo: "s3:https://example.com/backups"
  roles:
    - role: "couchdb"
    - role: "backup"
    - role: "alloy"
    - role: "ufw"
