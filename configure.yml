---
- name: "Development machine configure"
  gather_facts: true
  hosts:
    - desktop.local
    - macbook.local
    - laptop.local
  roles:
    - role: "ufw"
    - role: "locales"
    - role: "timezone"
    - role: "font"
    - role: "logind"
    - role: "bash"
    - role: "man"
    - role: "which"
    - role: "jq"
    - role: "ag"
    - role: "tcpdump"
    - role: "whois"
    - role: "nmap"
    - role: "usbutils"
    - role: "libheif"
    - role: "tmuxinator"
    - role: "rsync"
    - role: "gpg"
    - role: "git"
    - role: "git-crypt"
    - role: "zoxide"
    - role: "dotfiles"
    - role: "password_store"
    - role: "wireguard"
    - role: "vim"
    - role: "mise"
    - role: "neomutt"
    - role: "go"
    - role: "python"
    - role: "nodejs"
    - role: "docker"
    - role: "podman"
    - role: "ansible"
    - role: "alacritty"
    - role: "btop"
    - role: "claude"
    - role: "firefox"
    - role: "obsidian"
    - role: "localsend"
    - role: "gnome-calendar"
    - role: "gnome-newsflash"
    - role: "gnome-planify"
    - role: "gnome-loupe"
    - role: "gnome-papers"
    - role: "gnome-disk-utility"
    - role: "prometheus-node-exporter"
    - role: "alloy"
    - role: "gpu_driver"
    - role: "openlogi"
    - role: "backup"
      when: inventory_hostname == "desktop.local"
  tasks:
    - name: "Configure distro roles"
      when: ansible_facts.distribution == role.distro
      tags: always
      ansible.builtin.include_role:
        name: "{{ role.name }}"
        apply:
          tags: "{{ role.tags | default([]) }}"
      loop:
        - { name: "yay", distro: "Archlinux" }
        - { name: "mpv", distro: "Archlinux" }
        - { name: "hyprland", distro: "Archlinux" }
        - { name: "zen-browser", distro: "Archlinux" }
        - { name: "chromium", distro: "Archlinux" }
        - { name: "steam", distro: "Archlinux" }
        - { name: "gnome", distro: "Ubuntu" }
      loop_control:
        loop_var: "role"

- name: "Raspberry Pi configure"
  gather_facts: true
  hosts:
    - rpi5.local
  roles:
    - role: "ufw"
    - role: "pihole"
    - role: "locales"
    - role: "timezone"
    - role: "bash"
    - role: "man"
    - role: "jq"
    - role: "ag"
    - role: "tcpdump"
    - role: "whois"
    - role: "btop"
    - role: "gpg"
    - role: "git"
    - role: "git-crypt"
    - role: "dotfiles"
    - role: "password_store"
    - role: "vim"
    - role: "wireguard"
    - role: "gateway"
    - role: "wg_portal"
    - role: "prometheus-node-exporter"
    - role: "fritzbox_6660_exporter"
    - role: "alloy"
    - role: "neomutt"
    - role: "mount"
    - role: "garage"
    - role: "garage-webui"
    - role: "awscli"
    - role: "wakeonlan"

- name: "Proxmox VE configure"
  gather_facts: true
  hosts:
    - pve.local
  roles:
    - role: "ufw"
    - role: "pve"
    - role: "pve-storage"
    - role: "locales"
    - role: "timezone"
    - role: "logind"
    - role: "bash"
    - role: "man"
    - role: "jq"
    - role: "ag"
    - role: "whois"
    - role: "btop"
    - role: "gpg"
    - role: "git"
    - role: "git-crypt"
    - role: "dotfiles"
    - role: "password_store"
    - role: "vim"
    - role: "prometheus-node-exporter"
    - role: "alloy"
    - role: "restic"
    - role: "ethtool"
  tasks:
    - name: "Provision LXC containers"
      tags: ["lxc"]
      ansible.builtin.include_role:
        name: "pve-lxc"
        apply:
          tags: ["lxc"]
      vars:
        pve_lxc_id: "{{ hostvars[service_host].pve_lxc_id }}"
        pve_lxc_hostname: "{{ hostvars[service_host].pve_lxc_hostname }}"
        pve_lxc_template: "{{ hostvars[service_host].pve_lxc_template }}"
        pve_lxc_disk: "{{ hostvars[service_host].pve_lxc_disk }}"
        pve_lxc_cores: "{{ hostvars[service_host].pve_lxc_cores }}"
        pve_lxc_memory: "{{ hostvars[service_host].pve_lxc_memory }}"
        pve_lxc_ip: "{{ hostvars[service_host].pve_lxc_ip }}"
        pve_lxc_gateway: "{{ hostvars[service_host].pve_lxc_gateway }}"
        pve_lxc_dns: "{{ hostvars[service_host].pve_lxc_dns }}"
        pve_lxc_raw_config: "{{ hostvars[service_host].pve_lxc_raw_config | default(omit) }}"
      loop: "{{ groups['containers'] }}"
      loop_control:
        loop_var: "service_host"

    - name: "Configure SSH on LXC containers"
      tags: ["lxc"]
      ansible.builtin.include_role:
        name: "pve-lxc-ssh"
        apply:
          tags: ["lxc"]
      vars:
        pve_lxc_id: "{{ hostvars[service_host].pve_lxc_id }}"
        pve_lxc_ssh_port: "{{ hostvars[service_host].pve_lxc_ssh_port }}"
        pve_lxc_ssh_private_key: "{{ hostvars[service_host].pve_lxc_ssh_private_key }}"
        pve_lxc_ssh_public_key: "{{ hostvars[service_host].pve_lxc_ssh_public_key }}"
      loop: "{{ groups['containers'] }}"
      loop_control:
        loop_var: "service_host"
